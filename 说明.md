# hw_4 实验与报告：说明与检查清单

本文档汇总**代码层面**（推理与 Dropout）、**实验设计**（子集 vs 全量）、**注意力热力图**（色条与跨图可比）、**环境复现**与**报告加分表述**，与 `train.py`、`analyze_ablation.py`、`visualize_attention.py`、`transformer_model.py` 及 `hw_4.tex` 一致。

---

## 1. 代码层面：推理模式与 Dropout

### 1.1 务必使用 `model.eval()`

在运行 `visualize_attention.py`、计算验证/测试 BLEU、或任何**不反向传播**的前向中，应对整模调用 **`model.eval()`**。

- 本仓库 `visualize_attention.py` 在加载权重后已调用 `model.eval()`；`encoder_self_attention_weights()` 内再次调用 `model.eval()`，避免调用方遗漏。
- `train.py` 中 `evaluate_bleu` / `greedy_decode` 路径在评估前对模型 `eval()`（请自行保持与作业扩展代码一致）。

### 1.2 `MultiHeadAttention` 里的 Dropout 会不会「弄脏」注意力矩阵？

实现见 `transformer_model.py` 中 `ScaledDotProductAttention`：`attn_weights = softmax(...)` 后接 `self.dropout(attn_weights)`。

在 PyTorch 中 **`nn.Dropout` 仅在 `training=True` 时随机置零**；`model.eval()` 下为**恒等映射**，返回的注意力权重与 softmax 输出一致，不会出现因随机丢弃导致的「噪声斑点」。

若推理时**忘记** `.eval()`，Dropout 仍可能作用于注意力权重，热力图会出现伪结构、误导「对角线/局部性」论证。

---

## 2. 实验逻辑层面：数据规模不一致（如何写才可信）

### 2.1 本仓库实际设置（请勿误写为 10%）

当前用于快速完成的 **无 PE（None-PE）** 训练为：

`python train.py ... --experiment --pos-encoding none --epochs 5 --use-residual --no-early-stopping`

其中 **`--experiment`** 在 `train.py` 中表示：在**固定 `seed`** 下，每个 epoch **随机使用 50% 训练句**（约 14500/29000），而非 10%。若课程或报告需要「10% 子集」，需在 `train.py` 中**另行改采样比例**并重新出图。

Baseline 参考曲线来自 **`runs/baseline_reference_metrics.csv`**（与 `BASELINE.md` 一致），对应**全量**训练日志整理。

### 2.2 图中如何标注「不可强行比绝对 BLEU」

`analyze_ablation.py` 在生成 **2.1 叠图**（`--out-fig-2-1`）与 **无 PE 单曲线**（`--out-fig-none-pe-only`）时：

- 底部增加**固定脚注**（常量 `FIG_FOOTNOTE_21`），说明 Baseline 为全量 reference、无 PE 为 `experiment_mode` 的 **50% 子集**；
- 增加**上轴**：`Relative training progress (% of plotted max epoch)`，与横轴 `Epoch` 对齐，便于读者理解「在各自前 N 个 epoch 内的相对进度」。

**报告建议（可直接写入 `hw_4.tex`）**：明确写出——受计算资源限制，无 PE 消融采用 **50% 子集/epoch** 做快速验证；Baseline 为全量训练。正文对比侧重**训练初期 loss 下降斜率与 BLEU 形态**，不对子集与全量上的 **BLEU 绝对值**做强行横向对比。此类说明可提升可信度（`hw_4.tex` 2.1 节已写入一段摘要）。

---

## 3. 可视化层面：Attention Map 的色条（Colorbar）

### 3.1 问题：自动 `vmin/vmax` 会掩盖「涣散」

若对每个子图仅用「当前矩阵 min/max」拉伸 colormap，在注意力接近均匀时，仍可能占满高亮区，**视觉上不显「稀释」**。

### 3.2 本仓库策略

`visualize_attention.py` 中 `plot_multihead_heatmap` 支持三种模式（`--attn-colorbar`）：

| 模式 | 含义 |
|------|------|
| `auto`（默认） | 有对比度时用分位数 + `turbo`；近似均匀时用相对全局均值的 `RdBu_r`，避免「假鲜艳」。 |
| `fixed01` | **`vmin=0`, `vmax=1`**（可用 `--attn-vmin` / `--attn-vmax` 覆盖）+ **`viridis`**，与「行 softmax 后权重 ∈ [0,1]」一致，**便于跨条件（如 sin vs none）对比整体明暗与稀释**。 |
| `deviation` | 强制画相对均值的偏差图。 |

**报告作图推荐**：对比 PE 开/关时，同一论文内优先统一使用：

```bash
python visualize_attention.py ... --attn-colorbar fixed01 --attn-vmin 0 --attn-vmax 1
```

---

## 4. 检查清单（Checklist）

| 检查项 | 状态 / 位置 | 目的 |
|--------|-------------|------|
| **Random seed** | `train.py` 中 `config["seed"]=42`，`set_seed()` 在训练入口调用 | 助教复现曲线一致 |
| **依赖** | `requirements.txt`（torch、torchtext、spacy、matplotlib、nltk、tqdm 等） | 一键配置环境；**Spacy 模型**仍需手动：`python -m spacy download de_core_news_sm en_core_web_sm` |
| **Best checkpoint 路径** | `train.py` 每次运行写入 `runs/run_<时间戳>/best_model.pt`；`visualize_attention.py --checkpoint` 与 `--run-dir` 指向**同一 run** | 避免演示时加载过期权重 |
| **LaTeX 插图路径** | `hw_4.tex` 使用 `\includegraphics{report/...}`；请在**含 `hw_4.tex` 的目录**下执行 `xelatex`（或调整路径前缀） | 避免 PDF 丢图 |
| **推理 eval** | 可视化 / BLEU 前 `model.eval()` | 避免 Dropout 污染注意力 |

---

## 5. 进阶分析（报告加分 Insights）：位置编码在做什么

可在 **2.1 位置编码消融** 中引用如下逻辑（配合 `report/fig_2_1_*.png`）：

> 位置编码在词嵌入上叠加**随位置变化的相位信号**，使自注意力能够区分 token 的**绝对/相对次序**。在仅有内容嵌入、缺少显式位置信息时，置换等价的表示空间会令句法、词序对齐变难。本实验无 PE 曲线表明：模型仍可能降低部分 loss，但相对全量 sin-PE Baseline，**BLEU 爬升与句法对齐**更易成为瓶颈——与「失去位置信号后难以建立稳定句法结构」的叙述一致。

（具体数值以当前 `metrics.csv` 与 `BASELINE.md` 为准。）

---

## 6. 常用命令速查

```bash
cd hw_4

# 2.3 表 + 消融图 + Baseline 15 epoch 监控图
python analyze_ablation.py --out-fig report/fig_ablation_2_3_ep1_4.png --out-fig-monitor report/fig_baseline_train_monitor.png

# 2.1 图（含脚注与相对进度上轴）
python analyze_ablation.py --none-pe-csv report/none_pe_experiment_metrics.csv \
  --out-fig-none-pe-only report/fig_2_1_no_pe_loss_bleu.png \
  --out-fig-2-1 report/fig_2_1_vs_baseline_ref_early.png --early-epochs 5

# 注意力（固定色条，跨 sin/none 可比）
python visualize_attention.py --checkpoint runs/<run>/best_model.pt --run-dir runs/<run> \
  --test-index 0 --layer 5 --device cpu --out report/attn.png \
  --attn-colorbar fixed01 --attn-vmin 0 --attn-vmax 1
```

---

*文档随代码更新；若训练脚本中 `experiment_mode` 采样比例变更，请同步修改 `FIG_FOOTNOTE_21` 与本说明中的百分比。*
