# HW4 Transformer 实验进展总结

**文档日期**：2026-05-13（星期二）  
**适用目录**：`hw_4/`（Multi30k 德→英、与 `BASELINE.md` / `runs/` 一致）

---

## 一、时间线（便于对照仓库与 `runs/`）

| 时间（约） | 事项 |
|------------|------|
| 2026-05-11 及前后 | 完成训练管线、`transformer_model.py` 与 `train.py` 对齐作业；本地数据、词表、BLEU、`runs/` 落盘、早停等 |
| 2026-05-12～05-13 | **2.3 无残差消融** 完整跑通并落盘至 `runs/run_1778600084145839068/`；`visualize_attention.py`；`BASELINE.md` 增补训练监控段落 |

---

## 二、当前成果（可引用、可验收）

- **Baseline 数字与写法**  
  - 文件：`BASELINE.md`  
  - 要点：约 15 epoch；**best valid BLEU ≈ 18.7**（epoch 6–8）；epoch 1 **train loss/token ≈ 5.42**；epoch 12 后 loss 回升与 valid 下滑的**过拟合 / Noam 学习率阶段**分析（见该文档 **§6**）。

- **2.3 残差消融（最近一次完整训练）**  
  - 目录：`runs/run_1778600084145839068/`  
  - 配置要点：`use_residual=false`，`pos_encoding_type=sin`（与 Baseline 仅差残差），全量训练集。  
  - 产物：`best_model.pt`、`metrics.csv`、`train.log`、`summary.json`、`config.json`、`best_model_meta.json`。  
  - 训练过程：**计划 5 epoch**，**第 4 epoch 后早停**（valid BLEU 长期近 0，连续 3 epoch 未超过 epoch 1 的「最佳」）。  
  - 与 Baseline 对比（前向优化难度）：**epoch 1 train loss/token ≈ 5.66** vs Baseline **≈ 5.42**，无残差时**首轮降损更慢**。

- **加分项：注意力可视化**  
  - 脚本：`visualize_attention.py`  
  - 作用：加载 `best_model.pt`，对德语句画 **Encoder 多头自注意力**热力图；在 **sin 训练权重**下可再作**推断期关闭 PE** 的第二次前向，对比对角线统计（脚本内打印 `diag/off`）。

---

## 三、已完成的工程与实验工作（条例）

1. **模型（`transformer_model.py`）**  
   - 残差开关 `use_residual`（False 时为 `LayerNorm(Dropout(Sublayer(x)))`，无 `x+`）。  
   - 位置编码 `sin` / `none`（`none` 时不加 PE）。  
   - `qkv_mode`：`standard` / `shared`（作业 2.2 伏笔）。  
   - `build_transformer_from_config` 与训练配置对齐。

2. **训练（`train.py`）**  
   - 本地 `dataset-master`、spacy 分词、词表、掩码、验证/测试 BLEU。  
   - 每次运行新建 `runs/run_<时间戳>/`，写入 `metrics.csv`、`train.log`、`config.json`、`best_model.pt` 等。  
   - 早停、CLI（如 `--use-residual`、`--pos-encoding`、`--epochs`、`--no-early-stopping`）。

3. **文档**  
   - `BASELINE.md`：基线表、消融表占位、**§6 训练监控**（报告可直接引用）。

4. **可视化**  
   - `visualize_attention.py`：`matplotlib` 画多头热力图；注意 **无 PE 训练的 checkpoint 无法安全叠加 sin 表**，脚本内已有说明。

---

## 四、与 Baseline 的差异（写报告用一句话逻辑）

| 项目 | Baseline | 当前 2.3 消融 run |
|------|----------|-------------------|
| 残差 | 有 | **无** |
| 位置编码 | sin | sin（控制变量） |
| epoch 1 train loss/token | ≈ 5.42 | ≈ 5.66（更慢） |
| 若干 epoch 后 BLEU | 可达 ~18（valid） | 本 run 仅 4 epoch 且 BLEU≈0（**不宜与 Baseline 比绝对 BLEU**，比 **前几轮 loss 斜率** 即可） |

**结论方向**：在 **N=6** 深层设置下，**残差提供恒等路径**，有利于前向信号与反向梯度；无残差时**优化更难**，与「首轮 loss 更高」一致。

---

## 五、明天建议事项（按优先级）

1. **报告 2.3**：用 `metrics.csv` + `BASELINE.md` 表，对比 **epoch 1～3（或 1～4）** 的 `train_loss_token`；说明早停原因；若需「固定满 5 epoch」可加 `--no-early-stopping`。  
2. **（可选）对称对照跑**：`--use-residual` + 同 `epochs` / 同机，与无残差表并列。  
3. **加分项图**：对选定 `best_model.pt` 运行 `visualize_attention.py`，保存 **sin / none（推断期）** 图并写对角线差异。  
4. **配置收尾**：若日常以 Baseline 为主，可将 `train.py` 默认 `use_residual` 改回 `True`，消融时用 CLI 显式关闭。

---

## 六、环境与注意事项（2026-05-13）

- **CUDA**：本机曾出现驱动与 PyTorch 不匹配，脚本会 **退回 CPU**；全模型 512/6 层训练较慢属正常。  
- **进程排查**：`pgrep "python.*train.py"` 可能误匹配 `py_compile .../train.py`；以 **`runs/*/train.log` 是否更新**、`metrics.csv` 行数为准更可靠。

---

*本文档由实验记录整理而成；数值以 `BASELINE.md` 与 `runs/run_1778600084145839068/` 内文件为准。*
